import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function PackagesScreen() {
  return (
    <PlaceholderScreen
      icon={{ sf: 'airplane', md: 'flight' }}
      title="Packages"
      message="Browse travel packages here soon."
    />
  );
}
